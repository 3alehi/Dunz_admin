import { ICalendarProps } from '@kasraghoreyshi/calendar';

interface DatePickerOnChange {
    onChange?: (newDate: Date) => void;
}
interface IDatePickerProps extends Omit<React.InputHTMLAttributes<HTMLInputElement>, "onChange">, DatePickerOnChange {
    autoUpdate?: boolean;
    defaultDate?: Date;
    calendarProps?: ICalendarProps;
    date?: Date;
    dateFormat?: string;
    persianDigits?: boolean;
}
declare const DatePicker: ({ autoUpdate, calendarProps, onChange, defaultDate, dateFormat, date: controlledDate, persianDigits, ...props }: IDatePickerProps) => JSX.Element;

export { DatePicker };
