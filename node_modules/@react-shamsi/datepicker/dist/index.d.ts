import { ICalendarProps } from '@react-shamsi/calendar';

interface DatePickerOnChange {
    onChange?: (newDate: Date) => void;
}
interface IDatePickerProps extends Omit<React.InputHTMLAttributes<HTMLInputElement>, "onChange">, DatePickerOnChange {
    autoUpdate?: boolean;
    defaultDate?: Date;
    calendarProps?: ICalendarProps;
    date?: Date;
    dateFormat?: string;
    persianDigits?: boolean;
    calendarPortalElement?: HTMLElement | null;
}
declare const DatePicker: ({ autoUpdate, calendarProps, onChange, defaultDate, dateFormat, date: controlledDate, persianDigits, calendarPortalElement, ...props }: IDatePickerProps) => JSX.Element;

export { DatePicker };
